function calcularMedia(n1, n2) {
    let media = (n1, n2) / 2;
    return;
}

function apresentar() {
    console.log(`Olá, Mundo!`);
}

// console.log(`Média do Aluno: ${calcularMedia(5, 10)}`);
// apresentar()

const mediaAluno = (n1 = 0, n2 = 0, rep = 0) => {
    let media = (n1 + n2 + rep) / 3;
    return media
};

console.log(`Média do Aluno: ${mediaAluno(6, 7, 8).toFixed(1)}`);

